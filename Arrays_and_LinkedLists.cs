using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Permissions;
using System.Text;
using System.Threading.Tasks;

namespace Data_Structures_and_Algorithms_Review_Project
{
    class Arrays_and_LinkedLists
    {
        /*Notes:
         * -Arrays have fixed size
         * -Index is O(1)
         *      -Getting any random item is easy
         * -Insert is O(n) 
         *      -Have to move items after index to the right
         * -Remove is O(n)
         *      -Have to move items after index to the left 
         * -Try to use arrays when you need to randomly access items     
         */
    }
}
